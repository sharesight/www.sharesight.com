<!DOCTYPE html>
<html lang="<%= current_locale_obj[:lang] || 'en' %>" class="no-js">
  <!-- index layout -->
  <%= partial "layouts/partials/head", locals: locals.merge({}) %>

  <% partial "layouts/partials/body", locals: locals.merge({
    hero: "partials/index_hero"
  }) do %>
    <%= yield %>
  <% end %>
</html>
